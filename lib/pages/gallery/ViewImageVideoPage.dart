import 'package:flutter/material.dart';

class ViewImageVideo extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Image/Video'),),
      body: Center(child: Text('Image/Video Here'),),
    );
  }
}